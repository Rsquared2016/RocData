#--------------------------------------------#
                     ||
                  || || ||
               || || || || ||
            || || || || || || ||  
            || || || || || || ||
         
# <%= hostname %>
# <%= lsbdistdescription %>
# Get ready to start spewing lots of knowledge.
# <3 @androwis.
#--------------------------------------------#
